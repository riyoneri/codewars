function workOnStrings(a, b) {
  //..
}

// If you meet character in b which exists in a change its case
// Reverse inputs and then follow the same formula
// Return the string consisting of the changed version of a followed by changed version of b